import { FilterCancelTaskPipe } from './filter-cancel-task.pipe';

describe('FilterCancelTaskPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterCancelTaskPipe();
    expect(pipe).toBeTruthy();
  });
});
